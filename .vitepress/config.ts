import { defineConfig } from 'vitepress';

export default defineConfig({
  // サイトの基本設定
  title: 'VitePress',
  description: 'Just playing around.',
  base: "/vitepress-blog/",

  themeConfig: {
    // テーマ設定
  }
});